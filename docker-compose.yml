version: '3.8'
services:
  scheduler:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./data:/data
      - .:/app
    environment:
      - FLASK_APP=app.py
      - FLASK_ENV=development
    command: flask run --host=0.0.0.0
